import { ballAnimate } from "../Services/ballAnimation.js";

const game = {
  ball: {
    top: "560px",
    left: "560px",
  },
};
ballAnimate(game);
console.log(game);
ballAnimate(game);
ballAnimate(game);

ballAnimate(game);

ballAnimate(game);

ballAnimate(game);

ballAnimate(game);
console.log(game);
